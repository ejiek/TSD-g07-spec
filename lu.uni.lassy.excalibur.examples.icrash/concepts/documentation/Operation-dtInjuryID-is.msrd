@@Operation
icrash.concepts.primarytypes.datatypes.dtInjuryID.is
@description
" used to determine which strings are considered as valid injury identifiers."
//postFunctional descriptions
@postF 
" if the length of the value attribute of a dtInjuryID is a ptInteger greater than zero 
and lower or equal to 20 then the operation returns the ptBoolean true, else the ptBoolean false."
@endPostF

